import 'package:flutter/material.dart';

class kitsuScreen extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      appBar: new AppBar(
        title: new Text("Kitsu API"),
      ),
    );
  }

}